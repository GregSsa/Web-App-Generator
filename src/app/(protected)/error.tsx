"use client"; import { Button } from "@/components/ui/button"; export default function ErrorPage({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <div className="grid min-h-72 place-items-center text-center"><div><h2 className="text-xl font-semibold">Impossible de charger cette page</h2><p className="mt-2 mb-4 text-sm text-muted-foreground">Vérifiez votre connexion puis réessayez.</p><Button onClick={reset}>Réessayer</Button></div></div>; }
