import Link from "next/link";
import { signOut } from "@/actions/auth";
import { Button, buttonVariants } from "@/components/ui/button";
import { AppNavigation } from "@/components/layout/app-navigation";
export function AppShell({ children, email }: { children: React.ReactNode; email?: string }) { return <div className="min-h-dvh min-w-0"><aside className="fixed inset-y-0 left-0 z-40 hidden w-60 flex-col overflow-hidden border-r bg-card p-5 lg:flex"><Link href="/dashboard" className="mb-8 flex items-center gap-3 text-lg font-semibold"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground">MT</span><span className="truncate">Manga Tracker</span></Link><nav className="space-y-1"><AppNavigation /></nav><div className="mt-auto space-y-2"><p className="truncate text-xs text-muted-foreground">{email}</p><form action={signOut}><Button variant="outline" className="w-full">Se déconnecter</Button></form></div></aside><div className="min-w-0 pb-20 lg:ml-60 lg:pb-0"><header className="sticky top-0 z-30 flex h-16 min-w-0 items-center justify-between gap-3 border-b bg-background/90 px-4 backdrop-blur lg:px-8"><Link href="/dashboard" className="min-w-0 truncate font-semibold lg:hidden">Manga Tracker</Link><span className="hidden text-sm text-muted-foreground lg:inline">Votre bibliothèque personnelle</span><Link href="/mangas/new" className={buttonVariants({ size: "sm" })}>Ajouter</Link></header><main className="mx-auto w-full max-w-7xl min-w-0 p-4 lg:p-8">{children}</main></div><nav aria-label="Navigation principale" className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-6 border-t bg-background/95 pb-[env(safe-area-inset-bottom)] backdrop-blur lg:hidden"><AppNavigation mobile /></nav></div>; }
