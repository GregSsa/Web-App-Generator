import Link from "next/link";
import { BookOpen, Folder, Home, Library, Medal, Settings } from "lucide-react";
import { signOut } from "@/actions/auth";
import { Button, buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
const links = [{ href: "/dashboard", label: "Accueil", icon: Home }, { href: "/unread", label: "Non lus", icon: BookOpen }, { href: "/mangas", label: "Bibliothèque", icon: Library }, { href: "/categories", label: "Catégories", icon: Folder }, { href: "/ranking", label: "Classement", icon: Medal }, { href: "/settings", label: "Paramètres", icon: Settings }];
export function AppShell({ children, email }: { children: React.ReactNode; email?: string }) { return <div className="min-h-dvh lg:grid lg:grid-cols-[240px_1fr]"><aside className="hidden border-r bg-card p-5 lg:flex lg:flex-col"><Link href="/dashboard" className="mb-8 flex items-center gap-3 text-lg font-semibold"><span className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground">MT</span>Manga Tracker</Link><nav className="space-y-1">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={cn(buttonVariants({ variant: "ghost" }), "w-full justify-start")}><Icon className="size-4" />{label}</Link>)}</nav><div className="mt-auto space-y-2"><p className="truncate text-xs text-muted-foreground">{email}</p><form action={signOut}><Button variant="outline" className="w-full">Se déconnecter</Button></form></div></aside><div className="min-w-0 pb-20 lg:pb-0"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b bg-background/90 px-4 backdrop-blur lg:px-8"><Link href="/dashboard" className="font-semibold lg:hidden">Manga Tracker</Link><span className="hidden text-sm text-muted-foreground lg:inline">Votre bibliothèque personnelle</span><Link href="/mangas/new" className={buttonVariants()}>Ajouter</Link></header><main className="mx-auto w-full max-w-7xl p-4 lg:p-8">{children}</main></div><nav aria-label="Navigation principale" className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-6 border-t bg-background/95 pb-[env(safe-area-inset-bottom)] backdrop-blur lg:hidden">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="flex min-h-16 flex-col items-center justify-center gap-1 text-[10px] text-muted-foreground"><Icon className="size-5" /><span>{label}</span></Link>)}</nav></div>; }
