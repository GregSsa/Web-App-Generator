import { describe, expect, it } from "vitest"; import { extractedMangaMetadataSchema } from "@/lib/validations/manga";
describe("metadata schema", () => { it("accepte une sortie structurée", () => expect(extractedMangaMetadataSchema.safeParse({ mangaTitle: "Test", canonicalUrl: "https://example.com", sourceName: "example.com", latestChapter: null, confidence: .5, warnings: [] }).success).toBe(true)); it("rejette une confiance hors limites", () => expect(extractedMangaMetadataSchema.safeParse({ mangaTitle: null, canonicalUrl: null, sourceName: null, latestChapter: null, confidence: 2, warnings: [] }).success).toBe(false)); });
